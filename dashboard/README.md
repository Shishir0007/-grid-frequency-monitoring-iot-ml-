# Node-RED Dashboard

`node_red_dashboard_screenshot.png` shows the live dashboard streaming real-time
frequency data from the ESP32 hardware over MQTT.

The original Node-RED flow export (JSON) was not preserved from the original project —
this screenshot documents what it looked like.